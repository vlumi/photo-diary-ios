// Placeholder — the real API client, auth flow, instance registry, and
// todo-pin store land here in follow-up PRs (see ROADMAP.md).
import Foundation

public enum PhotoDiaryCore {
    /// Placeholder version marker for the scaffold. Real versioning
    /// comes from the app's MARKETING_VERSION.
    public static let scaffoldVersion = "0.0.1"
}
